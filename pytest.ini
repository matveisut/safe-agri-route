[pytest]
testpaths = backend/tests backend
markers =
    integration: requires a live ArduPilot SITL instance (tcp:127.0.0.1:5760)
    docker: requires Docker CLI installed (no running stack needed)
    stack: requires a running docker-compose stack on localhost:8000/3000
